import { Route, Routes } from 'react-router-dom';
import { RouteProps } from 'routes';
// const renderPage = (routes: RouteProps[]) => {
//     return <Route path={routes.map(item: RouteProps)=>item.path}
//     elements={item.elements} />
// };
